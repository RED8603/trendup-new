const projectController = require('./project.controller');
const documentController = require('./document.controller');
const changeHistoryController = require('./changeHistory.controller');

module.exports = {
    projectController,
    documentController,
    changeHistoryController,
};
