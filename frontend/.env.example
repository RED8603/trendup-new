# Frontend Environment Variables
# Copy this file to .env and fill in the values
# NEVER commit .env file to version control

# API Configuration (REQUIRED)
# Backend API base URL - MUST be set in production
VITE_API_URL=http://localhost:3001/api/v1
VITE_BACKEND_URL=http://localhost:3001

# Socket Configuration (REQUIRED)
# WebSocket server URL - defaults to VITE_BACKEND_URL if not set
VITE_SOCKET_URL=http://localhost:3001

# WalletConnect Configuration (REQUIRED)
# Get your project ID from https://cloud.reown.com/
# NEVER commit your actual project ID to the repository
VITE_WALLET_CONNECT_PROJECT_ID=your-walletconnect-project-id-here

# Environment
# Set to 'production' for production builds
VITE_ENV=development

# Example Production Values:
# VITE_API_URL=https://api.trenduplive.com/api/v1
# VITE_BACKEND_URL=https://api.trenduplive.com
# VITE_SOCKET_URL=https://api.trenduplive.com
# VITE_WALLET_CONNECT_PROJECT_ID=your-actual-project-id
# VITE_ENV=production
